/**
 * Re-exports the unified ReportsView from the transaction feature.
 * Both TransactionPage and TransactionDetailPage now share the same
 * API-powered report component to avoid code duplication.
 */
export { default } from '../reports/ReportsView';
